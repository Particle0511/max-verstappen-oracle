name: Oracle AI Engine CI/CD

on:
  push:
    branches:
      - main
    paths:
      - 'services/oracle-ai-engine/**'
  pull_request:
    paths:
      - 'services/oracle-ai-engine/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        working-directory: ./services/oracle-ai-engine
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run tests (Placeholder)
        working-directory: ./services/oracle-ai-engine
        run: |
          # We will add actual tests here later
          echo "Running tests..."

      - name: Log in to the GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: ghcr.io/${{ github.repository }}/oracle-ai-engine

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./services/oracle-ai-engine
          file: ./services/oracle-ai-engine/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Deploy (Placeholder)
        run: |
          # This will be configured later to deploy to our server
          echo "Deploying..."
          echo "Image: ghcr.io/${{ github.repository }}/oracle-ai-engine:latest"
